# URL du serveur WebSocket
WS_URL=wss://blender-coordinator.louisgelas-gamer.workers.dev/ws/vm

# Mot de passe VM (sera automatiquement défini par set_id.py sur Binder)
VM_PASSWORD=

# Exécutable Blender (par défaut: 'blender' dans le PATH)
BLENDER_EXECUTABLE=blender

# Intervalle heartbeat en secondes (défaut: 3)
# Doit être < timeout Worker / 3 pour garantir la marge
HEARTBEAT_INTERVAL=3

# Taille des chunks de cache en bytes (défaut: 32 KB)
# Sur connexion lente (<1 Mbps), garder ≤ 32768
# Sur connexion rapide (>10 Mbps), peut monter à 262144 (256 KB)
CHUNK_SIZE=32768

# Nombre maximum de tentatives de reconnexion
MAX_RECONNECT_ATTEMPTS=10

# Délai entre les reconnexions en secondes
RECONNECT_DELAY=5
